// const submitButtonModal = document.querySelector("#submit-button-modal")

// submitButtonModal.addEventListener("click", (event) => {
//   event.target.preventDefault;
//   const bookingDate = document.querySelector(".schedule-btn.btn.btn-primary.btn-xs.active").children[0].value;
//   const bookingTime = document.querySelector(".schedule-btn.btn.btn-primary.btn-xs.active").children[0].dataset.time
//   const bookingDatetime = bookingDate + " " + bookingTime
//   const modal = document.querySelector(".modal-body")

//   modal.insertAdjacentHTML("afterend",'<input id="prodId" name="field_id" type="hidden" value= ${bookingDatetime}');
//   event.target.submit;
// })
